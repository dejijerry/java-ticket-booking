# Backend - Ticket Booking Service

This is the Spring Boot backend for the Concurrent Ticket Booking Service.

## Run with Maven

```
cd backend
mvn spring-boot:run
```

## Build Docker image

```
docker build -t tickets-backend:latest .
```
