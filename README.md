# Concurrent Ticket Booking - Full Repository

This repository contains backend (Spring Boot) and frontend (Angular) projects and a docker-compose to run both.

Run with Docker Compose:

```
docker-compose up --build
```

Backend: http://localhost:8080
Frontend: http://localhost:4200
